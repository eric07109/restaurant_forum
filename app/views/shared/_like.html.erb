<% if not restaurant.is_liked?(current_user)%>
	<%= link_to "Like", like_restaurant_path(restaurant), method: :post, class: "btn btn-primary"%>
<%else%>
	<%= link_to "Unlike", unlike_restaurant_path(restaurant), method: :delete, class: "btn btn-primary"%>
<%end%>